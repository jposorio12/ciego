import HeaderCount from "./HeaderCount";
import HeaderDetail from "./HeaderDetail";
import HeaderDetailId from "./HeaderDetailId";

const useHeaders = () => {
  return { HeaderCount, HeaderDetail, HeaderDetailId };
};

export default useHeaders;
